# 实习生 B 技术实现说明

本文件记录已落地的技术契约，避免与实施计划重复。后续 M1～M6 按阶段追加实际实现说明。

## M0：运行时请求契约

### 位置与边界

- 代码包：`com.flowmind.platform.api.request`，位于 `platform-core`。
- 使用 Java 8 JavaBean：公开无参构造及标准 getter/setter；可手写访问器，也可使用 Lombok `@Data`，禁止使用 `record` 或 Java 9+ API。
- M0 不包含流程流转、持久化、SPI、Starter 自动装配、事件或输入校验逻辑。

### 请求继承

```text
OperationRequest
├─ 实例级请求
│  ├─ StartProcessRequest
│  ├─ UpdateVariablesRequest
│  ├─ TerminateProcessRequest
│  ├─ DeleteProcessInstanceRequest
│  ├─ JumpNodeRequest
│  └─ ForceCompleteRequest
└─ TaskOperationRequest
   ├─ SubmitTaskRequest
   ├─ ApproveTaskRequest / RejectTaskRequest / ReturnTaskRequest
   ├─ WithdrawTaskRequest / DirectSendRequest
   ├─ TransferTaskRequest / AddSignRequest
   └─ ClaimTaskRequest / UnclaimTaskRequest / RemindTaskRequest
```

- `OperationRequest`：`operationId`，所有状态修改请求共用。
- `TaskOperationRequest`：继承 `OperationRequest`，增加 `taskId`、`expectedTaskVersion`（`Long`）、`operatorUserId`、`comment`。
- `JumpNodeRequest` 是实例级请求，只含实例 ID 与目标节点等字段，不继承任务基类，也不携带任务版本。

### 关键字段

| 类型 | 已冻结字段 |
| --- | --- |
| `StartProcessRequest` | 流程编码、业务键、实例标题、发起人、发起部门、变量 |
| `UpdateVariablesRequest` | 实例 ID、操作人、变量 |
| `SubmitTaskRequest` | 任务基类字段、变量、`List<AttachmentUploadItem>` |
| 定向动作 | 驳回、直送、跳转使用 `targetNodeCode`；转办使用 `targetUserId`；加签使用 `addSignUserIds` |
| 实例操作 | 终止、删除、跳转、强制办结均包含 `instanceId`；需要操作人/意见的请求显式携带相应字段 |
| `AttachmentUploadItem` | 附件模板编码、归属类型、文件名、内容类型、大小、`byte[] content` |

`AttachmentUploadItem` 位于 `com.flowmind.platform.api.request`，是上传请求的内存载荷，不表示文件存入 SQLite；后续实现须通过文件存储 SPI 保存实际内容。

### 契约测试

- `OperationRequestContractTest` 验证两个请求基类的继承与 JavaBean 属性。
- `RuntimeRequestContractTest` 在 B0.2/B0.4 为原计划 17 个运行时修改请求提供有效夹具；B0.5 接入 A/C 契约后又纳入实例附件保存、任务附件保存、附件删除和告警处理 4 类请求；后续补充会写入提醒/告警的超时扫描请求，当前共覆盖 22 类。所有夹具统一验证 `operationId`，任务级请求额外验证 `taskId`、`expectedTaskVersion`、`operatorUserId` 和 `comment`。
- 测试覆盖每个请求的专有字段，并独立验证 `AttachmentUploadItem` 的全部六个属性及二进制内容。
- 验证命令：`mvn -q -pl platform/platform-core -am test`。

### 夹具汇总与覆盖门禁（B0.4）

- `RuntimeRequestContractTest` 以 17 个计划内运行时修改请求的类型集合为清单，并与参数化夹具的类型集合比较；集合不一致或出现重复夹具时测试失败。
- 每个请求夹具都需提供非空 `operationId`；任务级夹具还需提供 `taskId`、`expectedTaskVersion` 和 `operatorUserId`。`JumpNodeRequest` 单独断言不继承 `TaskOperationRequest`。
- `RuntimeResultContractTest` 保持对 `TaskActionResult.archivedTasks`、`createdTasks` 和 `replayed` 的验证。测试只验证 DTO 可表达性，不引入运行时幂等、版本校验或流程执行逻辑。

### 运行时结果 DTO（B0.3）

- 代码包：`com.flowmind.platform.api.dto`，同样采用 Java 8 JavaBean。
- `ProcessInstanceDTO` 表达实例标识、定义与流程信息、发起信息、正式实例状态、当前节点、变量、起止时间和创建出的 `TaskDTO` 列表。
- `TaskDTO` 表达节点、候选人、受理人、委托人、任务组/分支、正式任务状态、`taskVersion` 及时间字段；`HistoryTaskDTO` 表达归档任务、办理方式、动作类型、操作关联、变量快照、意见和完成时间。
- `TaskActionResult` 聚合 `operationId`、实例、归档/新建任务及重放标识；`OperationResult` 表达操作号、目标 ID、删除和重放标识。
- `RuntimeResultContractTest` 覆盖上述 DTO 的 JavaBean 属性、任务版本、任务归档/创建关系与操作结果标识；附件上传项由 `RuntimeRequestContractTest` 独立覆盖。
- 实例/任务状态、历史任务办理方式与动作类型、`OperationResult.targetType` 已全部使用正式枚举，不保留 `String` 兼容字段。

### 与 A 的模型及乐观锁契约对齐（B0.5）

- 本地 `feature/m0-b-dto-contract` 先快进到 `origin/develop@376dfd0`，再合并定义管理动作提交至 `origin/develop@3f0ffa3`；当前基线包含 A/B/C 的共享契约、SPI、Starter Service 接口和测试基线。
- `process_active_task` 仅通过 `definition_id` 关联具体流程定义，不重复保存定义版本；SQLite Schema、`ProcessActiveTaskEntity` 和 `TaskDTO` 均不包含 `version`。实例自身仍通过 `process_instance.version` 和 `ProcessInstanceDTO.version` 保存启动时的定义版本快照。
- `TaskDTO.taskVersion` 为 `Long`，对应 `ProcessActiveTaskEntity.lockVersion`；任务级请求通过同为 `Long` 的 `TaskOperationRequest.expectedTaskVersion` 原样回传该值。
- `RuntimeResultContractTest` 通过反射冻结 `ProcessInstanceDTO.version`、`TaskDTO.taskVersion`、`HistoryTaskDTO.operationId/comment` 与 A 持久化实体及任务请求的对应字段类型，同时继续以真实 setter/getter 调用验证 Lombok 生成的 JavaBean 契约。
- `FlowPlatformSchemaTest.activeTaskReferencesDefinitionWithoutDuplicatingItsVersion` 使用 `PRAGMA table_info(process_active_task)` 正向断言 `definition_id/lock_version`、反向断言不存在 `version`；Repository 集成测试按删除后的六列活动任务结构插入数据。
- `RuntimeRequestContractTest` 的当前唯一清单包含 22 类运行时/外围修改请求；相较 B0.4 的 17 类历史基线，B0.5 新接入 `SaveInstanceAttachmentRequest`、`SaveTaskAttachmentRequest`、`DeleteAttachmentRequest` 和 `HandleAlertRequest`，后续又纳入 `TimeoutScanRequest`。五类均提供非空 `operationId`，其中任务级的 `SaveTaskAttachmentRequest` 还提供 `expectedTaskVersion`。
- 已执行 `mvn -q -pl platform/platform-core -am test` 和根目录 `mvn -q test`；合并后联合测试共 139 项通过，0 失败、0 错误、0 跳过，子 agent 终审未发现阻断问题。
- `001_init_flow_platform.sql` 使用 `CREATE TABLE IF NOT EXISTS`，已由旧脚本创建的本地 SQLite 库不会自动删除原列；M0 未引入迁移框架，需要重建旧本地库后再验证新结构。
- B0.5 已完成正式枚举、公共 SPI 分层、附件授权失败关闭、`WorkflowEvent`/回调 DTO 单一契约及重复字段清理，并通过联合测试与逐项子 agent 审查。

### 公共 SPI 数据契约分层（B0.5）

- `AttachmentUploadItem` 是请求的嵌套二进制载荷，位于 `api.request`。
- `FileContent`、`StoredFile` 是文件存储 SPI 的读取/写入结果，`ProcessMessage` 是消息发布 SPI 的传输载荷，三者均位于 `api.dto`。
- `persistence.entity` 仅承载数据库映射实体；公共 `api.spi` 方法签名不得依赖该包。
- `PublicSpiContractTest` 通过反射冻结 `FileStorageProvider` 和 `MessagePublisher` 的公共参数、返回值，并拒绝暴露持久化包类型。

### 正式枚举收口（B0.5）

- 直接复用现有 `InstanceStatusEnum`、`TaskStatusEnum`、`ActionTypeEnum`、`DefinitionStatusEnum`、`ActivationStatusEnum`、`GrayStatusEnum`、`NodeTypeEnum`、`ApproverRuleTypeEnum`、`MultiInstanceModeEnum`、`AttachmentOwnerTypeEnum` 和 `WorkflowEventTypeEnum`。
- v4 已冻结有限值且目录中确实没有同义枚举的字段，新增 `HandleTypeEnum`、`OperationTargetTypeEnum`、`AttachmentConfigStatusEnum`、`AttachmentTemplateStatusEnum`、`ReminderTypeEnum`、`AlertTypeEnum`、`AlertSeverityEnum`。
- `FormalEnumContractTest` 冻结 21 个公共字段的声明类型及新增枚举值；`AuditLogDTO` 与 `AuditLogQuery` 统一通过 `OperationTargetTypeEnum + targetId` 表达流程定义、实例、任务或附件目标。
- `ProcessFormFieldDTO.fieldType/controlType` 是可演进的前端元数据，`ProcessMessage.messageType` 是宿主扩展类别；二者没有核心枚举闭集，继续使用 `String`。
- `WorkflowEvent.eventType` 和 `CallbackLogDTO.eventType` 以 `WorkflowEventTypeEnum` 为唯一 Java 契约；删除字符串重载、适配 setter、code getter 及枚举 `fromCode` 方法。序列化边界直接使用枚举名称，持久化实体仍可按数据库列保存字符串并在映射层显式转换。
- 回调失败信息严格映射 v4 的 `process_callback_log.last_error`：`CallbackLogDTO` 仅公开 `lastError`，不再维护会造成双字段序列化和状态同步的 `errorMessage` 别名。
- 定义管理动作由 `DefinitionActionTypeEnum` 表达，写入日志时转换为 `DEFINITION_*`；运行时动作仍来自 `ActionTypeEnum`。为统一 `process_operation_record` 与 `process_audit_log` 的单列存储，`OperationRecordDTO.actionType` 和 `AuditLogDTO.actionType` 使用 `String`，并提供原始字符串及两类枚举的重载 setter。
- 合并 `AuditLogDTO` 时保留 `OperationTargetTypeEnum targetType` 与 `targetId`，因此定义管理审计同时具备规范动作值和明确目标；`OperationLogContractTest` 覆盖运行时动作、定义动作、字符串存储和目标字段。
- 三个同名 setter 会让源码调用 `setActionType(null)` 时产生重载歧义，调用方需显式指定类型；原始 `String` 入口的合法性由上层映射和 Schema 约束保证，M0 不额外引入运行时校验。
- SQLite CHECK 约束已接受完整 `DEFINITION_*` 动作集合；旧数据库因 `CREATE TABLE IF NOT EXISTS` 不会自动更新，需要重建或迁移后使用。

### 附件访问失败关闭（B0.5）

- Starter 始终为缺失的 `AttachmentAccessProvider` 注册拒绝全部访问的后备实现；关闭本地 Mock 不会产生隐式放行。
- `AttachmentAccessGuard` 是附件 Service 的统一授权边界，只有 SPI 明确返回 `true` 才允许继续；SPI 缺失、返回拒绝或抛出运行时异常均返回 `false`。
- 单元测试分别覆盖缺失、拒绝、异常和明确允许；附件业务实现必须在调用 `FileStorageProvider` 之前通过 Guard。

### 已知阶段边界

- M0 的夹具只证明 DTO 可表达有效请求，不代替运行时非空、权限、幂等或乐观锁校验。
- 运行时校验、幂等执行、任务状态变更、附件 SPI 调用和数据库操作留待后续阶段实现。

## M1：架构纠偏、图索引与发布校验

原 B1.1 曾实现 `RuntimeDefinitionReadPort` 和 `RuntimeDefinitionInput`，历史提交为
`26839f2`、`f1df349`，当时 146 项测试及子 agent 复审均通过。

后续架构复审确认该读取层与 M0 已冻结的
`ProcessDefinitionService#getDefinition(String) -> ProcessDefinitionDetailDTO` 重复，因此不再
作为有效 M1 设计。B1.1 已在 `e25fcc7` 删除该读取层，改为：

- `DefinitionModelValidator` 直接接收正式详情 DTO；
- `DefinitionGraphIndex` 是 `core.validation` 中公开、`final` 的只读索引，供定义校验和 M2 运行时推进共同使用，提供节点/连线编码、入/出边和开始/结束/网关只读视图；
- 索引以 `sortOrder`、编码及校验相关字段确定顺序，不修改输入 DTO；重复编码模型仍报告问题，后续规则采用规范顺序中的末项；
- 入边与出边按各自端点独立索引，保留断裂边的既有聚合校验语义；空集合、空元素和空端点不会导致未捕获异常；
- `ProcessDefinitionCache` 缓存正式详情 DTO 副本，由定义写侧失效，并在 M2 由运行时 Service 读取。

`DefinitionGraphIndexTest` 覆盖稳定只读视图、空输入、半有效边与乱序异常模型。`platform` 下
`mvn -q test` 152 项通过；子 agent 复审通过。历史提交仅保留追溯价值。

### B1.2～B1.4：发布校验

`d435e1e` 将校验结果继续聚合到正式 `ValidationResult`，并新增稳定错误码：

- 基础结构：空白编码、`START`/`USER_TASK` 的唯一可解析出线，以及网关不得携带审批人或多人办理配置；
- 网关拓扑：排他网关的出线/条件/默认线，双向配对的并行分支和汇聚，以及每条并行分支到配对汇聚的路径；
- 有向环：M1 不求值表达式、未定义循环次数，无法静态证明环路有限结束，因此拒绝任意有向环；
- 扩展配置：表单字段编码唯一；附件配置组内模板 ID、附件编码唯一；适用节点存在；必填数量和 `minCount <= maxCount` 合法。

`DefinitionModelValidatorM1Test` 覆盖结构、网关、环路、表单和附件的正常/异常路径。`platform` 下
`mvn -q test` 163 项通过；子 agent 首轮发现空白连线编码、并行分支环路和普通路径环路遗漏，修复后复审通过。

### B1.5：完整定义缓存

`3f15cb0`、`35c58b9` 在 `core.definition` 新增并校正 `ProcessDefinitionCache`，不依赖 Service 或 Repository：

- 键为定义 ID；定义 ID 已唯一定位固定版本，版本保留在 DTO 中供 M2 与实例快照核对。缓存写入和读取均创建正式 `ProcessDefinitionDetailDTO` 的深层副本，包含节点、连线、表单、附件及附件内嵌列表；
- 仅发布、激活且发布校验通过的详情可写入，草稿、未激活、校验失败和缺少主键的详情不会影响已缓存的激活版本；
- M2 读侧未命中后先 `captureGeneration`，再加载和校验详情，并将令牌传给 `put`；令牌仅保护缓存新鲜度，不增加读取 port；
- 写侧在事务提交后调用 `invalidate`，回滚不失效。同一定义的 `put` 与 `invalidate` 以世代监视器串行化，失效返回后旧加载结果不能回写。

`ProcessDefinitionCacheTest` 覆盖命中/缺失、版本隔离、状态门禁、防御性复制、并发读写，以及锁存器编排的在途旧加载失效。`platform` 下 `mvn -q test` 170 项通过；子 agent 首轮发现旧加载回写竞态，修复后最终复审通过。

### B1.6：定义管理联合校验收口

- `DefaultProcessDefinitionService.validateForPublish` 固定走 `getDefinition(definitionId)`，再调用 `DefinitionModelValidator.validate(detail)`；管理端读取当前数据库事实，不读取运行时缓存，并将全部问题以正式 `ValidationResult` 返回。
- `saveGraph` 已移除完整发布图校验。草稿可以保存待发布预检发现的图问题；保存侧仍保留请求、定义状态、表单/附件引用及 SQLite 约束等最低完整性校验。
- `DefaultProcessDefinitionServiceTest` 使用真实 SQLite Repository、A 的整图保存与 C 的表单/附件配置，覆盖合法定义通过、错误连线引用草稿保存，以及两次预检返回完全相同的 `ValidationResult`（包括问题数量、错误码、消息、节点和连线定位）。
- 已执行 `mvn -q -pl platform/platform-core -am test` 和根目录 `mvn -q test`；缓存运行时读侧仍在 M2 接入，发布/激活生命周期仍在 M3 收口。

## M2：运行时持久化原语（第一步）

- `ProcessDefinitionRepository.findActiveFullByProcessCode` 只服务新实例启动：严格筛选 `PUBLISHED`、`ACTIVE`、`gray_status=OFF` 的全量激活定义。SQLite 的局部唯一索引保证同一流程编码最多一条该状态的定义；运行中实例不使用此查询重新选择版本。
- `ProcessInstanceRepository` 提供实例插入、按 ID 读取、变量 JSON 与当前节点 JSON 的状态受限覆盖，以及从 `RUNNING` 到 `COMPLETED` 的办结原语。变量读取、浅合并和幂等语义由后续 Runtime Service 在事务内协调，仓储不重复实现业务规则。
- `ActiveTaskRepository` 与 `TaskGroupRepository` 在保留既有乐观锁 CAS 更新的前提下，补充插入、按 ID 读取和实例维度的活动记录统计；活动任务与历史任务分别以 `created_at + id`、`completed_at + id` 稳定排序。
- `HistoryTaskRepository` 只增写入归档快照，拒绝空 `operationId`，并让表上的 `(active_task_id, action_type, operation_id)` 唯一约束作为重复归档的最后防线。
- `RuntimeRowMappers` 集中映射四张运行时主表；`RuntimeJsonCodec` 使用 Jackson Java Time 模块处理变量、候选人、当前节点和分支状态 JSON。空白或 JSON 字面量 `null` 统一映射为空集合，候选人/节点数组必须全部为字符串，避免 Jackson 隐式类型转换污染运行时数据。
- `RuntimeModelMapper` 负责实体到 `ProcessInstanceDTO`、`TaskDTO` 和 `HistoryTaskDTO` 的枚举及 JSON 字段转换。任务节点名称和委托人展示名称仍由上层基于定义图和用户上下文补充。
- 新增 SQLite 集成测试覆盖定义选择、实例状态门禁、活动任务/任务组过滤与排序、历史唯一约束；新增 JSON/DTO 映射测试覆盖 Java Time、空值和异常结构。已执行 `mvn -q test`（`platform`）通过；子 agent 审查后补齐 JSON 边界回归。

## M2：定义加载、请求校验与幂等执行框架（第二步）

- `RuntimeDefinitionLoader` 不引入第二套定义读取接口。新实例通过 `ProcessDefinitionRepository.findActiveFullByProcessCode` 选择已发布、激活且非灰度的唯一版本，随后始终以 `definitionId` 读取 `ProcessDefinitionCache`；缓存缺失时先捕获世代令牌，读取 `ProcessDefinitionService#getDefinition` 的完整详情并执行 `DefinitionModelValidator`，确认仍可启动后再回填缓存。运行中实例绕过流程编码选版，只按冻结的 `definitionId` 加载，并核对 `processCode/version` 快照；已停用定义的缓存缺失读取不会回填缓存。
- `RuntimeRequestValidator` 依赖 `CurrentUserProvider` 作为身份真源。它校验启动、变量更新和任务操作的必填字段及当前用户一致性，限制变量更新到 `NOT_STARTED/RUNNING`，限制任务操作到 `RUNNING` 实例和 `ACTIVE/CLAIMED` 任务，并校验候选人/受理人权限与任务乐观锁版本。`ApproverResolver` 的结果保留首次出现顺序、按用户 ID 去重且不得为空；M2 只接受 `SINGLE`。
- `RuntimeRequestHasher` 针对请求对象、继承字段、Map、集合、枚举和附件生成稳定 SHA-256。它排除 `operationId`，排序 Map 键；`byte[]` 只作为内容摘要参与哈希。因此相同业务请求可重放，Map 插入顺序和幂等号变化不改变摘要，附件内容变化会改变摘要且原始字节不会进入操作记录。
- `OperationIdempotencyService` 保持为唯一幂等实现。新增带 `instanceId/taskId` 的重载和兼容性绑定；首次插入的唯一键竞争会重新读取记录；过期 `PROCESSING` 记录通过 `processing_expires_at <= now` 的条件更新竞逐接管权，确保一个请求获胜。`beginOrReplay` 改为参与外层事务，`RuntimeOperationExecutor.markSuccess` 与后续业务写入由同一 Runtime Service 事务提交；成功重放经 `RuntimeJsonCodec` 恢复 DTO，任务结果额外标记 `replayed=true`。`markFailed` 仅供确定性业务失败调用，数据库中断等未知结果继续保留处理中租约。
- 新增 `RuntimeDefinitionLoaderTest`、`RuntimeRequestValidatorTest`、`RuntimeRequestHasherTest` 和 `RuntimeOperationExecutorTest`，并扩展 `OperationIdempotencyServiceTest` 覆盖唯一键竞争重读、条件接管、目标绑定与延后绑定。`mvn -q test`（`platform`）已通过；子 agent 复审未发现 P0/P1。后续第五步需要补充业务写入失败时 SUCCESS 记录同步回滚的事务集成测试，并在 REST 异常收口时统一目标绑定冲突的异常类型。

## M2：统一节点推进器与任务创建（第三步）

- `DefinitionGraphIndex` 已从校验器包内实现公开为可复用只读图索引；它保留原有 `sortOrder`、`edgeCode` 的确定性排序和不可变节点/连线/邻接视图。运行时不再另写节点或出线解析器。
- 新增 `ConditionExpressionEvaluator` SPI，契约为 `evaluate(String expression, Map<String, Object> variables)`。B 线不解释或拼接表达式；表达式语言、沙箱和生产实现仍由 A 线提供。未配置 SPI、SPI 抛错或网关结构异常均以 `FLOW_GATEWAY_CONFIG_INVALID` 失败关闭。
- `RuntimeNodeAdvancer.advanceToNode(instance, definition, targetNodeCode, taskGroupId, branchKey)` 是后续启动、提交和审批服务共用的推进入口，返回 `RuntimeAdvanceResult`（本次创建的任务与办结标志）。它不接管历史归档、幂等结果或事务开启，后者保留给第四、五步的动作服务和事务执行器。
- `RuntimeAdvancePreparation` 是推进的前置只读阶段。它在任何运行时行变更、尤其是任务 CAS 前，遍历即将经过的自动节点，使用既有 `RuntimeRequestValidator.resolveApprovers` 与 `ApproverResolver` 解析并去重所有可达 `USER_TASK` 的候选人，M2 只接受 `SINGLE`。正式推进阶段只消费这份不可变准备结果，不会在 CAS 后调用外部审批人 SPI；并行路径的任务仍继承 `taskGroupId/branchKey`。变量和审批人规则配置均以 `RuntimeJsonCodec` 读取，脏 JSON 映射为冻结定义错误。
- `EXCLUSIVE_GATEWAY` 同样在准备阶段按共享图索引的稳定出线顺序计算非默认条件，选择首条命中；无命中时使用唯一默认线，仍无可用出线返回 `FLOW_GATEWAY_NO_MATCH`。选中的目标节点写入准备结果，正式推进直接复用，避免同一表达式在 CAS 前后重复求值。每条自动路径维护独立访问集，重复自动节点或超过“节点数 + 连线数 + 1”上限返回配置错误，防止自动循环。
- `PARALLEL_SPLIT_GATEWAY` 创建 `PARALLEL_GATEWAY` 任务组，将每条出线编码写为 `RUNNING` 分支后分别推进；M2 显式拒绝嵌套并行。`PARALLEL_JOIN_GATEWAY` 校验任务组实例、配对汇聚点和分支状态，复用已有 `markBranchArrived` 条件更新。是否沿汇聚出线继续只由 CAS 前 `completedCount + 1 == totalCount` 决定，非最后分支即使在重读前被其他分支完成任务组也不会重复推进。
- `END` 仅在没有活动任务和活动任务组时清空 `currentNodeCodes` 并办结。其他推进结束后统一从数据库当前的 `ACTIVE/CLAIMED` 任务重算节点集合，避免串行、条件和并行路径维护不同的内存状态。
- `RuntimeNodeAdvancerTest` 覆盖候选任务创建、稳定条件路由、默认线、无匹配、表达式失败/缺失、自动环路、并行拆分、嵌套拒绝、部分/最后分支汇聚、并发交错完成防重复推进和 `END` 办结门禁。`mvn -q test`（`platform`）通过；独立审查首先发现并行汇聚的“重读后误判最后分支”竞态，修复并补测试后复审无 P0/P1。

## M2：六个 Runtime Service 方法（第四步）

- `DefaultProcessRuntimeService` 是 `ProcessRuntimeService` 的首个 M2 Bean。它只编排既有 Repository、`RuntimeDefinitionLoader`、`RuntimeRequestValidator`、`RuntimeOperationExecutor` 与 `RuntimeNodeAdvancer`；不复制定义选版、图解析、CAS 或幂等逻辑。M3/M5 的接口继续抛出明确的阶段未实现异常。
- `startProcess` 校验可信发起人、加载可启动定义后只插入 `NOT_STARTED` 实例，`startedAt/currentNodeCodes/createdTasks` 均为空语义，不调用推进器或回调。`startAndSubmit` 先在内存中构造 `RUNNING` 实例、初始变量快照并完成起始路径准备，再插入实例并从唯一 `START` 出线推进；返回值来自推进后的持久化实例，并携带首批新任务。
- 两个启动方法使用同一种 `StartProcessRequest`，因此在 `RuntimeOperationTypes` 中固定不同的幂等记录动作：`START` 表示仅建实例，`START_AND_SUBMIT` 表示创建并推进。后者只存在于 `process_operation_record.action_type`，流程事件和历史任务仍使用 `ActionTypeEnum.START`，避免把 Service 入口差异污染业务动作语义。`UPDATE_VARIABLES` 同理仅表示变量更新的幂等记录动作。
- 任务动作先通过 `validateTaskIdentity` 校验操作号、任务版本和可信当前用户，再读取幂等决定；成功重放直接恢复 `TaskActionResult(replayed=true)`，不再读取已终态活动任务。首次执行才读取任务/实例、校验权限和冻结定义。这样同一请求在任务 CAS 成功后重试不会重复附件、历史、下一任务或回调。
- `submitTask` 限定 `STARTER` 用户任务。它按提交项构造正式 `SaveTaskAttachmentRequest` 调用附件服务，再调用 `CheckAttachmentRequest` 校验必传附件；二者都在 `ActiveTaskRepository.complete` 之前。随后基于合并后的内存变量完成后继路径准备（审批人 SPI 与条件选择均在此时发生），才执行任务 CAS；CAS 成功后写变量、归档 `SEND/NORMAL` 历史快照，并以准备结果推进。`approve` 限定非 `STARTER` 用户任务，不接受变量更新，同样在 CAS 前完成后继路径准备，归档为 `APPROVE/NORMAL` 后走同一推进路径。
- `updateVariables` 仅允许 `NOT_STARTED/RUNNING` 实例，合并已保存 JSON 对象与请求 Map，状态条件更新成功后保存可重放 DTO。`getInstance` 按冻结定义补齐活动任务节点名称，聚合 Repository 已稳定排序的活动任务/历史任务，并把非空历史意见投影为 `ProcessCommentDTO`，不新增重复意见表。
- 服务为启动、任务创建、任务提交/完成和流程办结组装 `WorkflowEvent`，事件 ID 为 `operationId:eventType:targetId`；实际 Outbox 写入仍委托 `CallbackService` 的 C 线实现。跨线协作收口后，五个修改型入口已建立 Spring 事务边界，使 CAS、变量、归档、推进、Outbox 和幂等成功结果原子提交。
- 新库由 `001_init_flow_platform.sql` 创建时已接受 `START_AND_SUBMIT`、`UPDATE_VARIABLES`。为保证已有 SQLite 库可升级，新增一次性 `002_m2_runtime_operation_actions.sql`：在事务中复制原 `process_operation_record` 的全部字段与数据、替换动作 CHECK 约束并恢复四个索引。`schema/sqlite/README.md` 固定其部署前手动执行要求，迁移回归验证已有 `APPROVE` 记录保留且两个 M2 动作可写。
- `DefaultProcessRuntimeServiceTest` 覆盖六个方法的主路径、附件保存/校验先于任务 CAS、变量浅合并、历史/详情聚合及提交/审批的终态成功重放无副作用；`FlowPlatformSchemaTest` 覆盖新动作和旧库迁移。独立子 agent 首轮发现启动入口串用重放和 SQLite 旧库 CHECK 约束两项 P1，修复并复审后无 P0/P1。

## M2：跨线运行时协作收口

`DefaultProcessRuntimeService` 的 Spring 主构造器额外注入 C 线的 `RuntimeStateValidator` 与 `HistoryTaskWriter`。任务动作先由状态校验器读取并核对实例、任务状态和乐观锁版本，随后仍保留 B 的 `RuntimeRequestValidator` 做可信身份、候选人/受理人权限及节点动作语义校验；两者职责不重叠，也不替代 Repository 的 CAS。

任务 CAS 成功后，历史快照统一交由 `HistoryTaskWriter` 按 `(active_task_id, action_type, operation_id)` 幂等归档，再由 B 的节点推进器创建后续任务或办结实例。回调继续通过 `CallbackService.publishCallback` 写入 C 的 Outbox；五个修改型公开入口会先建立/判断幂等租约，再委托独立 `RuntimeTransactionExecutor` 执行主事务，因此历史、Outbox 与运行时主状态及成功结果同事务提交。保留兼容构造器仅供既有直接构造测试使用，Spring 生产装配固定使用完整构造器。

附件配置组的公共原语采用定义范围校验：保存草稿前校验同批配置的组 ID 一致性；null 配置列表按空列表处理；激活时若 `definitionId + attachmentConfigId` 找不到任何记录即返回冻结校验错误。该修正不会改变无附件定义可激活、或定义激活时唯一 DRAFT 配置组切换为 ACTIVE 的既有业务规则。

验证时将 `TEMP`/`TMP` 指向项目内临时目录，并以单一受限 Surefire fork 执行 `mvn -q -pl platform-core -DforkCount=1 "-DargLine=-Xms64m -Xmx384m -XX:MaxMetaspaceSize=192m" test`（`platform` 目录）；全量 `platform-core` 测试完成，Maven 退出码为 0。

## M2：事务、回调和跨线协作（第五步）

- `RuntimeTransactionExecutor` 使用独立 Spring 组件中的 `TransactionTemplate` 承载主事务，避免运行时 Service 的同类内部调用绕过事务代理；其默认传播语义为 `REQUIRED`，正常入口没有外层事务时创建事务，有外层业务事务时参与该事务以保持业务侧原子性。
- 幂等记录的创建/租约判断先于主事务独立完成；主事务只包含任务 CAS、变量更新、历史归档、任务组与后续任务、实例状态、C 线 Outbox 追加和 `markSuccess`。主事务失败后这些写入整体回滚；随后才将可确定的领域失败冻结为 `FAILED`，未知数据库故障仍保留 `PROCESSING` 租约。
- `DefaultProcessRuntimeServiceTransactionIntegrationTest` 组装真实 Repository、`DefaultProcessRuntimeService`、Spring 代理的 `DefaultCallbackService(MANDATORY)` 与 `CallbackOutboxService`。成功审批断言任务、历史、后续任务、两条 Outbox 和成功结果均落库；分别在历史归档、后继任务创建、Outbox 追加后注入异常，均断言任务 CAS、变量/历史、后继任务、Outbox 和 `resultJson` 全部回滚，幂等记录仅保留 `FAILED`。
- `TaskGroupRepository` 合并基线中存在重复 `findById` 和本地行映射，本步保留共享 `RuntimeRowMappers.TASK_GROUP` 的单一定义，恢复全模块编译。

## M2：测试与提交顺序进展

- 计划的 Repository、定义加载、幂等和节点推进测试分别由 `RuntimePersistenceRepositoryIntegrationTest` / `TaskRepositoryIntegrationTest`、`RuntimeDefinitionLoaderTest`、`OperationIdempotencyServiceTest` / `RuntimeOperationExecutorTest`、`RuntimeNodeAdvancerTest` 承担；后者覆盖条件路由、单层并行拆分/汇聚及最后分支防重复推进。
- `DefaultProcessRuntimeServiceTest` 新增任务 CAS 与审批人 SPI 的顺序断言：解析器失败时不执行 CAS，成功解析必须先于 CAS。`DefaultProcessRuntimeServiceTransactionIntegrationTest` 以真实 C 线 `DefaultCallbackService(MANDATORY)` 覆盖成功链路，并覆盖历史归档、后继任务创建及 Outbox 追加失败的全量回滚。
- `M2SerialWorkflowIntegrationTest` 以真实 SQLite Repository 运行 `startAndSubmit → submitTask → approve → approve`，断言流程办结、无活动任务、三条历史/意见、变量浅合并、详情聚合和八条回调记录。`M2GatewayWorkflowIntegrationTest` 以同一 Service 闭环覆盖条件网关的高/低分支，以及单层并行的首分支不推进、最后分支仅创建一个后继任务。
- `M2RuntimeConcurrencyIntegrationTest` 采用文件型 SQLite、WAL 和独立连接/Service，并发验证同一任务仅一方可完成，以及两个并行末分支均成功、汇聚后只创建一条后继任务；测试同时断言历史和回调数量。
- 在构建目录报告文件不可写的环境中，使用 `-Dmaven.compiler.useIncrementalCompilation=false` 配合 Surefire 的 `-DuseFile=false -DdisableXmlReport=true` 执行上述定向门禁。`M2SerialWorkflowIntegrationTest`、`M2GatewayWorkflowIntegrationTest`、`M2RuntimeConcurrencyIntegrationTest`、`DefaultProcessRuntimeServiceTransactionIntegrationTest`、`DefaultProcessRuntimeServiceTest` 与 `RuntimeNodeAdvancerTest` 均退出码 0；子 agent 在修正真实双分支并发断言后复审，无 P0/P1。
