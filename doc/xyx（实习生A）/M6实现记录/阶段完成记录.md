# 实习生A M6 阶段完成记录

记录日期：2026-07-29

## 完成范围

- 条件分支：沿用现有 `EXCLUSIVE_GATEWAY` 运行时路由实现，保持条件命中、默认出线和无匹配失败语义。
- 或签任务创建：`RuntimeNodeAdvancer` 已按 `multiInstanceMode=OR_SIGN` 创建 1 个 `OR_SIGN` 任务组和 N 条候选人活动任务。
- 或签唯一推进：`DefaultProcessRuntimeService.approve` 已通过 `completeOrSignGroup` 执行任务组 CAS，只有首个成功完成任务组的请求继续推进。
- 或签取消轨迹：获胜任务归档为 `APPROVE`，同组剩余开放任务更新为 `CANCELED` 并归档，统一放入 `TaskActionResult.archivedTasks`。
- 或签防退化修复：节点配置为 `OR_SIGN` 时必须读取或签任务组；任务组缺失返回 `FLOW_TASK_GROUP_NOT_FOUND`，任务组类型不是 `OR_SIGN` 返回 `FLOW_TASK_GROUP_STATUS_INVALID`，不再回落到普通审批路径。普通并行分支任务仍保留原 `taskGroupId/branchKey` 推进链路。

## 分工边界

- A 线完成条件分支复核与或签运行时主体实现。
- A 线不实现 `COUNTERSIGN` 会签闭环，不修改 `incrementCompletedCount` 的会签/加签计数语义。
- A 线不接管并行汇聚、管理员修复、超时处理、催办、异常告警。
- A 线不打开分组任务上的驳回、撤回、转办、加签、直送等增强动作。

## 测试补充

- `DefaultProcessRuntimeServiceTest.approveOrSignTaskCompletesGroupCancelsSiblingsAndAdvancesWithParentContext`
- `DefaultProcessRuntimeServiceTest.approveParallelGroupedUserTaskKeepsGenericBranchContext`
- `DefaultProcessRuntimeServiceTest.approveGroupedTaskFailsWhenTaskGroupIsMissing`
- `DefaultProcessRuntimeServiceTest.approveGroupedTaskFailsWhenTaskGroupTypeIsNotOrSign`
- `RuntimeNodeAdvancerTest.createsTaskGroupAndOneTaskPerApproverForOrSignNode`
- `TaskRepositoryIntegrationTest.orSignGroupCompletesOnceAndFindsRemainingOpenTasksByGroup`
- `M6OrSignConcurrencyIntegrationTest.concurrentOrSignCompletionsProduceExactlyOneWinner`
- `M6AdvancedFlowCrossLineSqliteIntegrationTest.advancedFlowCrossesConditionOrSignCountersignParallelTimeoutAndCallbackGovernance`

## 验证记录

已执行并通过：

```powershell
mvn -q -pl platform/platform-core -am "-Dtest=M6OrSignConcurrencyIntegrationTest,M6AdvancedFlowCrossLineSqliteIntegrationTest,M0M3CrossStageSpringBootIntegrationTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
mvn -q clean test
```

完整回归结果：76 个测试套件、490 项测试，0 失败、0 错误、0 跳过。

## 验收结论

- 或签任务组 CAS 已通过两个独立 SQLite 连接的并发竞争验证，结果严格为一个成功、一个失败，任务组终态为 `COMPLETED` 且锁版本只递增一次。
- A/B/C 跨线 SQLite 测试已验证真实定义发布与运行时链路中的排他条件、或签取消、会签全员完成、并行汇聚、节点超时告警和回调失败告警。
- A 线 M6 条件分支与或签范围完成验收；并发测试当前聚焦任务组持久化 CAS，运行时审批与兄弟任务取消由跨线测试和既有服务测试共同覆盖。
