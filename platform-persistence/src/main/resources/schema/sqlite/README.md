# SQLite Schema

This folder stores SQLite DDL used by `platform-demo` and local integration tests.

The first stage uses SQLite only. The demo database file is copied between interns when shared local data is needed.

