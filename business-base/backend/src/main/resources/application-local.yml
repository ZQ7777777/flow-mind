server:
  address: 127.0.0.1

flow-mind:
  platform:
    mock:
      enabled: true
    sqlite:
      path: ${FLOW_MIND_PLATFORM_SQLITE_PATH:./data/business-flow-local.db}
    callback:
      async-enabled: true
    timeout-scan:
      enabled: true
